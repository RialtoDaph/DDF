NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Powers the Franz assistant (src/app/api/franz/route.ts). Without this,
# Franz responds with a "not set up yet" message instead of erroring.
ANTHROPIC_API_KEY=
